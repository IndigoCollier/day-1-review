// ✅ JavaScript Quiz - Select the correct answer by replacing "?"

// 1. What does the following code return(A)
const nums = [1, 2, 3];
const result = nums.map(n => n * 2);
// A) [2, 4, 6]
// B) [1, 2, 3, 2, 4, 6]
// C) undefined
;

// 2. What is the value of x(B)
let x = 10;
x = x + "5";
// A) 15
// B) "105"
// C) NaN

// 3. Which loop is best to iterate over an array(A)
// A) forEach
// B) while
// C) do while


// 4. What keyword prevents reassignment(B)
// A) let
// B) const
// C) var
